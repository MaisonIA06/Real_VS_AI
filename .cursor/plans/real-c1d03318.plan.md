---
name: Architecture WebApp "Real vs AI"
overview: ""
todos:
  - id: ec571094-5b72-4ad4-82b2-4d5e1547680f
    content: Configuration Docker Compose (PostgreSQL, Django, React, Nginx)
    status: pending
  - id: 71b91c9e-d5f0-43bb-88da-62a4f9a619aa
    content: Setup Django + DRF avec structure du projet
    status: pending
  - id: c9440f74-1d1d-415c-b4ce-a692de0472d7
    content: Creation des modeles Django (Category, MediaPair, Quiz, GameSession, etc.)
    status: pending
  - id: bd660263-3ae2-459a-9615-599b9b6297eb
    content: Implementation API publique (sessions, answers, leaderboard)
    status: pending
  - id: 15912666-aae7-4007-be67-29f63be0606f
    content: Implementation API admin (CRUD categories, paires, quiz)
    status: pending
  - id: 81fe6d32-7491-4751-aff5-74e7cf59e667
    content: Setup React + Vite + TailwindCSS + Framer Motion
    status: pending
  - id: 01b8f041-e0c3-4b53-9dde-fe6051f0fc3c
    content: Pages principales (Home, Game, Result, Leaderboard)
    status: pending
  - id: d7dc4cf4-0c2c-4326-b4b7-6afce430ceef
    content: Composants de jeu (MediaDisplay, Timer, ProgressBar, Feedback)
    status: pending
  - id: 39a7d8e7-3351-42eb-a142-2a2c2e77ca1c
    content: Animations (swipe, feedback vert/rouge, confettis)
    status: pending
  - id: d8bc987f-445c-4f1b-a7e2-b1981ce186b1
    content: Interface admin React (Dashboard, MediaPairs, Quizzes)
    status: pending
  - id: f9545218-c610-402b-87b5-2ea790d37acd
    content: Configuration Nginx (reverse proxy, fichiers statiques)
    status: pending
---

# Architecture WebApp "Real vs AI"

## Structure du Projet

```javascript
WebApp/
├── docker-compose.yml
├── .env.example
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── manage.py
│   ├── config/
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── apps/
│   │   ├── game/
│   │   │   ├── models.py
│   │   │   ├── views.py
│   │   │   ├── serializers.py
│   │   │   └── urls.py
│   │   └── admin_api/
│   │       ├── views.py
│   │       ├── serializers.py
│   │       └── urls.py
│   └── media/
│       └── pairs/
├── frontend/
│   ├── Dockerfile
│   ├── package.json
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── hooks/
│   │   ├── services/
│   │   └── styles/
└── nginx/
    ├── Dockerfile
    └── nginx.conf
```



## Modeles Django

```mermaid
erDiagram
    Category {
        int id PK
        string name
        string description
        boolean is_active
        datetime created_at
    }
    
    MediaPair {
        int id PK
        int category_id FK
        string real_media_url
        string ai_media_url
        string media_type
        string difficulty
        string hint
        boolean is_active
        datetime created_at
    }
    
    Quiz {
        int id PK
        string name
        string description
        boolean is_random
        boolean is_active
        datetime created_at
    }
    
    QuizPair {
        int id PK
        int quiz_id FK
        int media_pair_id FK
        int order
    }
    
    GameSession {
        int id PK
        int quiz_id FK
        string session_key
        string pseudo
        int score
        int streak_max
        int time_total_ms
        boolean is_completed
        datetime created_at
    }
    
    GameAnswer {
        int id PK
        int session_id FK
        int media_pair_id FK
        boolean is_correct
        int response_time_ms
        int order
    }
    
    GlobalStats {
        int id PK
        int media_pair_id FK
        int total_attempts
        int correct_answers
    }
    
    Category ||--o{ MediaPair : contains
    Quiz ||--o{ QuizPair : has
    MediaPair ||--o{ QuizPair : included_in
    Quiz ||--o{ GameSession : generates
    GameSession ||--o{ GameAnswer : contains
    MediaPair ||--|| GlobalStats : tracks
    MediaPair ||--o{ GameAnswer : references
```



### Modeles detailles

**Category** : Categories thematiques (Paysages, Portraits, Animaux...)**MediaPair** : Paire de medias (reel + IA)

- `media_type`: "image" ou "video"
- `difficulty`: "easy", "medium", "hard"
- `hint`: Explication post-reponse

**Quiz** : Questionnaire personnalise

- `is_random`: Si true, pioche aleatoirement dans les paires

**GameSession** : Session de jeu d'un utilisateur

- `streak_max`: Plus longue serie de bonnes reponses
- `time_total_ms`: Temps total de jeu

**GlobalStats** : Statistiques par paire pour afficher "X% ont trouve"

## Endpoints API

### API Publique (Jeu)

| Methode | Endpoint | Description ||---------|----------|-------------|| GET | `/api/game/quizzes/` | Liste des quiz disponibles || POST | `/api/game/sessions/` | Demarre une session (retourne 10 paires) || POST | `/api/game/sessions/{id}/answer/` | Soumet une reponse || GET | `/api/game/sessions/{id}/result/` | Resultat final + stats || GET | `/api/game/leaderboard/` | Classement (filtrable par quiz) || POST | `/api/game/leaderboard/` | Enregistre pseudo + score |

### API Admin (Interface React)

| Methode | Endpoint | Description ||---------|----------|-------------|| GET/POST | `/api/admin/categories/` | CRUD categories || GET/POST | `/api/admin/media-pairs/` | CRUD paires de medias || POST | `/api/admin/media-pairs/upload/` | Upload fichiers medias || GET/POST | `/api/admin/quizzes/` | CRUD quiz || GET | `/api/admin/stats/` | Dashboard statistiques |

## Composants React

```mermaid
flowchart TB
    subgraph pages [Pages]
        Home[HomePage]
        Game[GamePage]
        Result[ResultPage]
        Leaderboard[LeaderboardPage]
        AdminDashboard[AdminDashboard]
        AdminPairs[AdminMediaPairs]
        AdminQuizzes[AdminQuizzes]
    end
    
    subgraph components [Composants]
        MediaDisplay[MediaDisplay]
        Timer[Timer]
        ProgressBar[ProgressBar]
        StreakIndicator[StreakIndicator]
        ScoreCard[ScoreCard]
        Confetti[ConfettiEffect]
        FeedbackOverlay[FeedbackOverlay]
        QuizSelector[QuizSelector]
        PseudoInput[PseudoInput]
    end
    
    subgraph admin [Composants Admin]
        MediaUploader[MediaUploader]
        PairEditor[PairEditor]
        QuizBuilder[QuizBuilder]
        StatsChart[StatsChart]
    end
    
    Game --> MediaDisplay
    Game --> Timer
    Game --> ProgressBar
    Game --> StreakIndicator
    Game --> FeedbackOverlay
    Result --> ScoreCard
    Result --> Confetti
    Result --> PseudoInput
    Home --> QuizSelector
    AdminPairs --> MediaUploader
    AdminPairs --> PairEditor
    AdminQuizzes --> QuizBuilder
    AdminDashboard --> StatsChart
```



### Bibliotheques Frontend

- **React Router** : Navigation
- **Framer Motion** : Animations (swipe, transitions, feedback)
- **canvas-confetti** : Effet confettis
- **Axios** : Requetes API
- **TailwindCSS** : Styling
- **React Query** : Cache et gestion d'etat serveur
- **Recharts** : Graphiques pour l'admin

## Configuration Docker

### docker-compose.yml

Services :

- **db** : PostgreSQL 15
- **backend** : Django + Gunicorn
- **frontend** : Node pour build, servi par Nginx
- **nginx** : Reverse proxy + fichiers statiques/medias

### Flux reseau

```mermaid
flowchart LR
    Client[Client Web] --> Nginx
    Nginx -->|/api/*| Backend[Django Backend]
    Nginx -->|/media/*| MediaFiles[Fichiers Medias]
    Nginx -->|/*| Frontend[React Build]
    Backend --> PostgreSQL[(PostgreSQL)]
    Backend --> MediaFiles
```



## Logique de Jeu

### Demarrage de session

1. L'utilisateur choisit un quiz (ou mode aleatoire)
2. Le backend genere une session avec 10 paires
3. Retourne les paires dans l'ordre (ou aleatoire si `is_random`)

### Soumission de reponse

1. L'utilisateur clique sur le media qu'il pense reel
2. Envoi : `{pair_id, choice: "left"|"right", response_time_ms}`
3. Backend retourne : `{is_correct, hint, global_stats, current_streak}`
4. Frontend affiche feedback + animation

### Calcul du score

- Base : 100 points par bonne reponse
- Streak bonus : +10 points par reponse consecutive (max +50)
- Time bonus : Points supplementaires si reponse < 5 secondes
- Score final = somme des points

## Phases d'Implementation

### Phase 1 : Infrastructure

- Configuration Docker Compose
- Setup Django + DRF
- Setup React + Vite + TailwindCSS
- Configuration Nginx

### Phase 2 : Backend Core

- Modeles Django
- API publique (jeu)
- API admin (CRUD)
- Upload de medias

### Phase 3 : Frontend Jeu

- Pages principales (Home, Game, Result)
- Composants de jeu (MediaDisplay, Timer, ProgressBar)
- Animations et feedback
- Leaderboard

### Phase 4 : Interface Admin

- Dashboard avec statistiques
- Gestion des paires de medias
- Creation de quiz
- Upload de fichiers

### Phase 5 : Polish

- Animations avancees (Framer Motion)
- Confettis et effets visuels